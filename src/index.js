import React from "react";
import ReactDOM from "react-dom/client";
import HealthReminderApp from "./App";
import "./index.css";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <HealthReminderApp />
  </React.StrictMode>
);
